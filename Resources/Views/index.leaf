#if(meta.userLoggedIn) {
  #set("content") {
    #embed("elections")
  }
} else {
  #set("content") {
    <h1>Welcome</h1>
    <h2>You can use this application to vote in the election.</h2>
    <p>If you'd like some information before you begin, select the 'Help' button in the navigation bar at the bottom of the screen.</p>
    <p>Otherwise, select 'Begin' to get started.</p>
    <a class='startButton' href='/login'>
      <h3>Begin</h3>
    </a>
  }
}

#embed("base")
